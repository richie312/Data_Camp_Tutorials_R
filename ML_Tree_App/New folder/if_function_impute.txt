if (input$Imputation == "Continous"){
      data()[i1]<- lapply(data()[i1], function(x) replace(x, is.na(x), mean(x[!is.na(x)])))
    }
    if (input$Imputation == "Categorical"){
      data()[i2]<- lapply(data()[i2], function(x) replace(x, is.na(x), mode(x[!is.na(x)])))
    }
    else 
      
        data()[i1]<- lapply(data()[i1], function(x) replace(x, is.na(x), mean(x[!is.na(x)])))
        data()[i2]<- lapply(data()[i2], function(x) replace(x, is.na(x), mode(x[!is.na(x)])))
    
    